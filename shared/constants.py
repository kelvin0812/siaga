"""Physical constants shared between the synthetic generator and the backend
feature builder, so they can't silently drift apart."""

TIP_RESOLUTION_MM = 0.2
"""Rainfall per tipping-bucket count — a common commodity gauge resolution."""
